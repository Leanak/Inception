services:
  mariadb:
    build: ./requirements/mariadb
    container_name: mariadb_container
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - inception_network
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 2s
      timeout: 2s
      retries: 20
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}

  wordpress:
    build: ./requirements/wordpress
    container_name: wordpress_container
    volumes:
      - wordpress_data:/var/www/html
    networks:
      - inception_network
    restart: always
    healthcheck:
      test: ["CMD", "pgrep", "php-fpm8.4"]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      mariadb:
        condition : service_healthy
    env_file:
      - .env
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      DOMAIN_NAME: ${DOMAIN_NAME}
      WP_ADMIN: ${WP_ADMIN}
      WP_ADMIN_PASSWORD: ${WP_ADMIN_PASSWORD}
      WP_ADMIN_EMAIL: ${WP_ADMIN_EMAIL}
  
  nginx:
    build:
      context: ./requirements/nginx
      args:
        DOMAIN_NAME: ${DOMAIN_NAME}
    container_name: nginx_container
    ports:
      - "443:443" 
    volumes:
      - wordpress_data:/var/www/html
    networks:
      - inception_network
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-k", "https://localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      - wordpress
        condition: service_healthy
    env_file:
      - .env

volumes:
  mariadb_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/lenakach/data/mariadb
  wordpress_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/lenakach/data/wordpress

networks:
  inception_network:
    driver: bridge
